{
  "name": "engine-futebol-workspace",
  "private": true,
  "workspaces": ["engine", "web", "scraper"],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace engine && npm run build --workspace scraper && npm run build --workspace web",
    "test": "npm test --workspace engine && npm test --workspace scraper && npm test --workspace web",
    "scrape": "npm run scrape --workspace scraper"
  }
}
