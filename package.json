{
  "name": "engine-futebol-workspace",
  "private": true,
  "workspaces": ["engine", "web"],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace engine && npm run build --workspace web",
    "test": "npm test --workspace engine",
    "calibrate": "npm run calibrate --workspace engine"
  }
}
