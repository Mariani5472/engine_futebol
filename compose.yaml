services:
  web:
    build:
      context: .
      dockerfile: web/Dockerfile
    container_name: tactic-manager-web
    ports:
      - "5173:5173"
  scraper:
    build:
      context: .
      dockerfile: scraper/Dockerfile
    container_name: tactic-manager-scraper
    ports:
      - "5174:5174"
